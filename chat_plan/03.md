[Task 3] <ChatMessageList> 컴포넌트 구현 (react-virtuoso 기반)

이제 Task 2에서 설계한 내용을 바탕으로,
실제 코드 파일을 생성/수정해서 Virtuoso 기반 ChatMessageList를 구현해줘.

요구사항:

1) 파일 생성/수정
   - ChatMessageList 컴포넌트를 새 파일로 만들거나,
     이미 존재한다면 해당 파일을 수정해라.
   - 경로는 다음 중 하나를 추천하고, 실제로 선택한 경로를 명시해라:
     - src/features/chat/components/ChatMessageList.tsx
     - 또는 현재 프로젝트 컨벤션에 맞는 위치.

2) 구현 내용
   - react-virtuoso에서 Virtuoso 컴포넌트를 import 해서,
     props로 받은 messages를 data로 넘긴다.:contentReference[oaicite:7]{index=7}
   - startReached 콜백에서 onLoadMoreBefore를 호출해서
     이전 페이지를 로딩한다 (단, isLoadingMoreBefore 일 때는 중복 호출 방지).
   - followOutput/alignToBottom 또는 수동 스크롤 상태 계산을 사용해서:
     - 사용자가 아래에 있을 때만 자동으로 새로운 메시지를 따라가게 한다.
   - itemContent에서 ChatBubble 같은 하위 컴포넌트를 사용해 메시지를 렌더링하고,
     가변 높이(여러 줄 텍스트, 이미지)를 가정해 특별한 높이 추정 없이도
     자연스럽게 렌더링되도록 한다.:contentReference[oaicite:8]{index=8}

3) 스크롤 UX 디테일
   - "맨 아래에 있는지 여부"를 onReachBottomChange 콜백으로 상위에 알려줄 수 있도록
     Virtuoso의 스크롤 위치를 기반으로 계산하는 로직을 구현해줘.
   - hasMoreBefore 가 false일 때는 startReached 에서 로딩을 더 하지 않도록 처리.
   - 로딩 중일 때 리스트 상단에 로딩 인디케이터(스켈레톤 or spinner)를 넣을 수 있는
     placeholder 영역을 만든다 (예: components.Header 같은 Virtuoso 커스텀 컴포넌트 활용).:contentReference[oaicite:9]{index=9}

4) 출력 형식
   - [수정/생성된 파일 경로]
   - 전체 컴포넌트 코드 (TypeScript)
   - 필요한 경우 함께 수정된 타입/헬퍼 함수 코드
   를 보여줘.
